# screen-time-app
an Android Screen Time Tracker App using React Native (Expo Bare Workflow) that shows how long the user spends on each app per day.        The app should:  collect daily app usage stats  identify device without authentication  store analytics in Firebase Firestore  display daily usage list
